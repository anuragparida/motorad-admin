import React from 'react';
import PropTypes from 'prop-types';


const Loader = () => (
  <div>
    Loader Component
  </div>
);

Loader.propTypes = {};

Loader.defaultProps = {};

export default Loader;
